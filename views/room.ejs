<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <script> const roomName = "<%= roomName %>";
        const name = "<%= name %>"; const card1 = "<%= card1 %>"; const card11 = "<%= card11 %>";
        const card2 = "<%= card2%>"; const card22 = "<%= card22%>"; const scoreBot = "<%= scoreBot %>"; let money = "<%= money%>";
        on_index = false;</script>
    <title>Blackjack | <%= roomName %></title>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link rel="icon" href="img/blackjack-icon.png">
    <script defer src="http://localhost:3000/socket.io/socket.io.js"></script>
    <script defer src="script.js"></script>
    <link rel="stylesheet" href="css/roomstyle.css" type="text/css">
    <link rel="stylesheet" href="css/style.css" type="text/css">
    <script src="js/jquery-3.5.1.min.js"></script>
    <link rel="stylesheet" href="css/bootstrap.min.css" type="text/css">
    <script src="js/bootstrap.min.js"></script>
</head>
<body>
<div class="horizontal-menu">
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
        <a class="navbar-brand" href="/"><img src="img/blackjack-icon.png" style="width:70px; height: 70px;"></a>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavAltMarkup"
                aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
            <div class="navbar-nav">
                <a class="nav-item nav-link" href="/">Home</a>
                <a class="nav-item nav-link" href="/rules">Rules</a>
                <form class="user-info" action="/logout?_method=DELETE" method="POST">
                    <p id="user-info" class="nav-item">User: <%= name %> | Money: <%= money %>$</p>
                    <button class="btn btn-outline-danger my-2 my-sm-0" type="submit">Sign Out</button>
                </form>
            </div>
        </div>
    </nav>
</div>
<div class="main-body">
    <div class="chat-body">
        <div class="chat">
            <div id="message-container"></div>
        </div>
        <div class="chat-area">
            <form id="send-container">
                <textarea type="text" id="message-input" required placeholder="Type message..."></textarea>
                <button type="submit" id="send-button">Send</button>
            </form>
        </div>
    </div>
    <div class="game">
        <h2>Dealer Cards</h2>
        <div id="dealer-cards">
        </div>
        <div id="timer">
            <h1 id="score">Score:</h1>
            <h1 id="counter">00:00</h1>
            <h1 id="win-money">Winner Money: </h1>
        </div>
        <h2>Your Cards</h2>
        <div id="player-cards">

        </div>
        <div id="actions-menu">
            <form id="next-card">
                <button id="next" type="submit">NEXT CARD</button>
            </form>
            <div id="winner-table">
                <h3 id="winner-text">Win/Lose</h3>
            </div>
        </div>
    </div>
</div>
</body>
</html>